// ".dialog-container" : { visible: false, opacity: 0 }
	// ".dialog-overlay": { backgroundColor: '#80000000' }
	// ".dialog-wrapper": { height: Ti.UI.SIZE, backgroundColor: '#fff', layout: 'vertical' }
	// ".dialog-wrapper[platform=ios]": { clipMode: Ti.UI.iOS.CLIP_MODE_ENABLED }